<!DOCTYPE html>
<html lang="es-AR">
  <head>
    <%- include("../partials/head.ejs") %>

    <title>Product Detail | Grupo Sie7e</title>

    <!--Hoja de estilos bootstrap de esta página-->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </head>
  <body>

    <div class="d-grid gap-2 fixed-top">
      <a href="../admin/products/create" class="btn btn-primary">Crear producto</a>
    </div>

    <div class="row">
        <% for (let i = 0; i < inventory.length; i++) { %>
            <form class="col-12 col-sm-6 col-md-6 col-lg-3 mb-3" method="post" action="/admin/products/<%= inventory[i].id %>?_method=DELETE">
                <div class="card" style="width: 18rem;">
                    <img src="<%= inventory[i].image %>" class="card-img-top" alt="<%= inventory[i].name %>">
                    <div class="card-body">
                        <h5 class="card-title"><%= inventory[i].name %></h5>
                        <p class="card-text"><%= inventory[i].description %></p>
                        <a href="../admin/products/<%= inventory[i].id %>/edit" class="btn btn-primary">Editar</a>
                        <button type="submit" class="btn btn-danger">BORRAR</button>
                    </div>
                </div>
            </form>
        <% } %>
    </div>

    <style>
        .card-img-top {
          width: 100%;
          height: auto;
          aspect-ratio: 1 / 1; /* Mantener la relación de aspecto cuadrada */
          object-fit: cover; /* Ajustar la imagen para cubrir el contenedor */
        }
      </style>
    </div>
  </body>
</html>
